
class TestClass {
    init() {
        Int a;
        a = 10;
        println(a);
    }

    method add(a, b) Int {
        return a + b;
    }

    method subtract(a, b) Int {
        return a - b;
    }

    method multiply(a, b) Int {
        return a * b;
    }

    method divide(a, b) Int {
        return a / b;
    }

    method isEqual(a, b) Boolean {
        return false;
    }

}

    Int x;
    x = 10;
    Int y;
    y = 20;
    TestClass test;
    test = new TestClass();
    Int z;
    z = test.add(x, y);
    println(z);
